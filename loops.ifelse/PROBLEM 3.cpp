#include <iostream>
using namespace std;
int main (){
	int num;
	cin >> num;
	if (num>0) {
cout << "num is positive";
	}
	else if (num<0) {
		cout << "num is negative";
	}
	else if (num == 0){
		cout << "not postive niether negative";
	}
		system ("pause");
		return 0;
	}
